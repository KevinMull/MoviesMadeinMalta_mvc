@model MaltaMoviesMVCcore.Models.Scene

@{
     ViewData["Title"] = @Model.Title.Title + " - " + Model.LocationSite.LocationPlaceAndSiteName;
}

<div class="modal-body">
    <button type="button" class="close" data-dismiss="modal">&times;</button>
    <h2> @Model.Title.TitleAndYear</h2>
    <p>
        <img src="@($"/images/scenes/large/{@Model.SceneId}.jpg")" alt="@Model.LocationSite.LocationPlaceAndSiteName" title="@Model.LocationSite.LocationPlaceAndSiteName" />
    </p>

    <strong>
        @Html.DisplayNameFor(model => model.LocationSite.LocationPlaceAndSiteName):
    </strong>

    @Html.DisplayFor(model => model.LocationSite.LocationPlaceAndSiteName) -

    <strong>
        @Html.DisplayNameFor(model => model.LocationAlias.LocationAliasCountryAndPlace):
    </strong>

    @Html.DisplayFor(model => model.LocationAlias.LocationAliasCountryAndPlace)
   

</div>
@*@using (Html.BeginForm("Scene", "Scenes", FormMethod.Post))
    {*@

<div class="row">
    <div class="col-md-5 col-md-offset-5">
        <button type="button" class="btn btn-default" data-dismiss="modal">
            Close
        </button>
        @*<button type="submit" id="approve-btn"
                    class="btn btn-danger">
                Save
            </button>*@
    </div>
</div>
@*}*@

<script type="text/javascript">
    $(function () {
        $('#approve-btn').click(function () {
            $('#modal-container').modal('hide');
        });
    });
</script>

